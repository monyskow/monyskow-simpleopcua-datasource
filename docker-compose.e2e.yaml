# =============================================================================
# Docker Compose - E2E Testing Setup
# =============================================================================
# This setup is used by Playwright E2E tests (npm run e2e).
# It provisions a single "OPC-UA Test Server" datasource that the tests expect.
#
# Usage:
#   npm run build && mage buildAll    # Build the plugin first
#   npm run e2e                        # Runs this compose automatically
#
# Or manually:
#   docker compose -f docker-compose.e2e.yaml up --wait
#   Open http://localhost:3000
#
# Test credentials (username/password auth): user1 / password1, admin / admin123
# =============================================================================

services:
  opcua-server:
    build:
      context: ./test-servers/node-opcua-server
      dockerfile: Dockerfile
    container_name: opcua-server
    hostname: opcua-server
    ports:
      - '4840:4840'
    environment:
      - PORT=4840
      # Set HOSTNAME to service name so the advertised endpoint URL is resolvable
      # within the compose network. node-opcua uses this in the endpoint descriptor.
      - HOSTNAME=opcua-server
      - SERVER_NAME=opcua-e2e
    healthcheck:
      test: ['CMD', 'nc', '-z', 'localhost', '4840']
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 15s
    restart: unless-stopped

  grafana:
    user: root
    container_name: 'monyskow-simpleopcua-datasource'
    build:
      context: .config
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-12.2.0}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
    volumes:
      # Plugin mounted for Grafana to load
      - ./dist:/var/lib/grafana/plugins/monyskow-simpleopcua-datasource
      # Source code mounted for development
      - .:/root/monyskow-simpleopcua-datasource
      # Provision E2E test datasource. AUTH_CONFIG selects the combination:
      # anon-none | userpass-none | userpass-b256-sign | cert-b256-sign | cert-aes256-sign
      - ./provisioning/datasources/datasources-e2e-${AUTH_CONFIG:-anon-none}.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.monyskow-simpleopcua-datasource:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: monyskow-simpleopcua-datasource
    healthcheck:
      # Grafana's health endpoint returns 200 once the HTTP server is up and
      # plugins are loaded. Without this, `docker compose up --wait` exits as
      # soon as the container starts — before Grafana actually serves requests.
      test: ['CMD-SHELL', 'wget -qO- http://localhost:3000/api/health || exit 1']
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    depends_on:
      opcua-server:
        condition: service_healthy
    restart: unless-stopped
